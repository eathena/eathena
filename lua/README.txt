This folder is where you place lua packages to be used by the servers.
By default, no other paths will be searched.

OPTIONAL:
If init.lua exists, it will be executed on startup to setup the global lua 
environment, treating any error as a fatal error that terminates the server.
